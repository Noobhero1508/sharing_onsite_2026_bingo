# Nguồn tài nguyên

- Logo FEV, ảnh chữ Sharing Onsite, nền và ba bánh răng: tài nguyên do người dùng cung cấp, giữ nguyên bản gốc trong `assets/images/`.
- Hình thẻ 6–25: sao chép từ bộ `internet_icons` do người dùng cung cấp. Thẻ 1–5 đang hiện ô số chờ hình; các ảnh tạm cũ còn trong thư mục nhưng không được hiển thị.
- Vỏ máy SVG, bộ biểu tượng, các cụm bánh răng SVG đang hiển thị và mã ứng dụng mới: dựng riêng cho bản cải tạo này. Ba ảnh bánh răng gốc vẫn được giữ trong thư mục tài nguyên để đối chiếu.
- Bản nghe thử `preview/16-am-thanh-luot-quay.mp3` được ghi từ bộ âm thanh tổng hợp của chính trang này; trang vẫn tự tạo âm khi chạy.
- GSAP 3.15.0, Draggable 3.15.0, Flip 3.15.0: các bản phân phối từ gói công khai `gsap`, lưu cục bộ; thông báo bản quyền giữ trong đầu tệp. [Giấy phép GSAP](https://gsap.com/standard-license/).
- Be Vietnam Pro Regular/SemiBold và Barlow Condensed SemiBold: Google Fonts, SIL Open Font License. Các giấy phép đi kèm trong `assets/fonts/`.

Ứng dụng không tải tài nguyên qua mạng trong khi chạy.
